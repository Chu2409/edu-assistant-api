name: Deploy Nous Backend (API + Worker)

on:
  push:
    branches:
      - main
      - develop

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set environment variables
        id: vars
        run: |
          if [[ "${{ github.ref }}" == "refs/heads/main" ]]; then
            echo "ENVIRONMENT=production" >> $GITHUB_OUTPUT
            echo "IMAGE_TAG=latest" >> $GITHUB_OUTPUT
          else
            echo "ENVIRONMENT=development" >> $GITHUB_OUTPUT
            echo "IMAGE_TAG=dev" >> $GITHUB_OUTPUT
          fi

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ghcr.io/chu2409/edu-assistant-api:${{ steps.vars.outputs.IMAGE_TAG }}

  # deploy-production:
  #   needs: build-and-push
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/main'
  #   environment: production # 🎯 Usa el environment de GitHub

  #   steps:
  #     - name: Trigger Portainer Webhook (API Service - Production)
  #       run: curl -s -X POST ${{ secrets.PORTAINER_WEBHOOK_API }}

  #     - name: Trigger Portainer Webhook (Worker Service - Production)
  #       run: curl -s -X POST ${{ secrets.PORTAINER_WEBHOOK_WORKER }}

  # deploy-development:
  #   needs: build-and-push
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/develop'
  #   environment: development # 🎯 Usa el environment de GitHub

  #   steps:
  #     - name: Trigger Portainer Webhook (API Service - Development)
  #       run: curl -s -X POST ${{ secrets.PORTAINER_WEBHOOK_API }}

  #     - name: Trigger Portainer Webhook (Worker Service - Development)
  #       run: curl -s -X POST ${{ secrets.PORTAINER_WEBHOOK_WORKER }}
